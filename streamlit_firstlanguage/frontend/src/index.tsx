import React from "react"
import ReactDOM from "react-dom"
import FirstLanguageNER from "./FirstLanguageNER"

ReactDOM.render(
  <React.StrictMode>
    <FirstLanguageNER />
  </React.StrictMode>,
  document.getElementById("root")
)
